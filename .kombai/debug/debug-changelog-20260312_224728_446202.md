# Debug Changelog — Session 20260312_224728_446202

## Changes Made

### 1. Removed auto-rotation from OrbitControls
- **File:** `src/components/fusion-ring-3d/FusionRing3D.tsx`
- **Change:** Removed `autoRotate` and `autoRotateSpeed` props from `<OrbitControls>`. Removed `isUserControlling` state and `setIsUserControlling` calls. Removed unused `isUserControlling` import of `useState`. Added `crunchSeed` state and `setCrunchSeed`. Updated `handleSpikeEruption` to accept `(sector: number, delta: number)` and trigger crunch when `delta > 0.55`. Added `useEffect` to trigger crunch when `visualState === 'PAIRING'`.
- **Revert:** Re-add `autoRotate={!isUserControlling}`, `autoRotateSpeed={0.4}`, `onStart/onEnd` handlers, restore `handleSpikeEruption(sector: number)` signature

### 2. Removed ghost ring self-rotation
- **File:** `src/components/fusion-ring-3d/GhostRings.tsx`
- **Change:** Removed `group.rotation.z += rotationDir * rotationSpeed * delta` from `useFrame`. Added `uCrunchIntensity` uniform and crunch spring animation (`crunchPhaseRef` state machine). `rotationDir` and `rotationSpeed` props kept in type signature but no longer used for animation.
- **Revert:** Restore `group.rotation.z += rotationDir * rotationSpeed * delta` in useFrame, remove crunch uniform/animation

### 3. Increased deformation scale + added crunch to vertex shader
- **File:** `src/shaders/fusion-ring/ring.vert.glsl`
- **Change:** Added `uniform float uCrunchIntensity`. After all deformations, shrinks all vertex positions toward origin via `pos = pos * (1.0 - uCrunchIntensity * 0.90)`. Added crunch turbulence noise `sin(uTime * 28.0 + vSectorIndex * 3.0) * uCrunchIntensity * 0.15` for energetic appearance at peak crunch.
- **Revert:** Remove `uniform float uCrunchIntensity` declaration and the crunch block at bottom of `main()`

### 4. Added crunch spring animation to RingMesh
- **File:** `src/components/fusion-ring-3d/RingMesh.tsx`
- **Change:** Added `crunchSeed` prop. Added `uCrunchIntensity: { value: 0 }` to uniforms. Changed `uDeformationScale` from `1.35` to `2.2`. Added `crunchPhaseRef` ('idle'|'crunching'|'releasing') and `crunchValueRef` for spring animation in `useFrame`. Added `useEffect` on `crunchSeed` to start crunch phase.
- **Revert:** Remove `crunchSeed` prop, remove `uCrunchIntensity` uniform, change `uDeformationScale` back to `1.35`, remove crunch refs and animation logic

### 5. DivergenceSpikes delta passthrough
- **File:** `src/components/fusion-ring-3d/DivergenceSpikes.tsx`
- **Change:** Updated `onSpikeEruption` type from `(sector: number) => void` to `(sector: number, delta: number) => void`. Updated both call sites to pass `event.delta`.
- **Revert:** Restore `(sector: number) => void` signature, remove `event.delta` from call sites

## Revert Status
- [ ] Change 1 - OrbitControls autoRotate
- [ ] Change 2 - GhostRings self-rotation
- [ ] Change 3 - Vertex shader crunch
- [ ] Change 4 - RingMesh crunch animation
- [ ] Change 5 - DivergenceSpikes delta passthrough
